#include<stdio.h>
int main()
{
int n,i,temp=1;
    scanf("%d",&n);
    if(i<0)
    {
        printf("error");
    }
    else
          for(i=1;i<=n;i++)
  {
      temp *= i;
}
 printf("factorial of %d",temp);
return 0;
}
